//var messageEncode = document.getElementById('msgEncode').value;
//var offsetEncode = document.getElementById('offsetE').value;

const cipher = {
//  encode: function cipherE(messageEncode, offsetEncode) {
//    let encodeResult = "";

//    for (let i = 0; i < messageEncode.length; i++) {
//      const code = messageEncode.charCodeAt(i);

//      if ((code >= 65) && (code <= 90)) {
//        encodeResult = ((code - 65 + offsetEncode) % 26) + 65;
//        encodeResult += String.fromCharCode(code);
//        return encodeResult;
//      } 
//      
//    }
// },
};

export default cipher;

